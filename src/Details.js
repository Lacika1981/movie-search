// import React, { Fragment, useEffect, useState } from 'react';
// import { Link, Router } from '@reach/router';

const Details = props => {
  console.log(props);
  return props.selected.Title;
};

export default Details;
